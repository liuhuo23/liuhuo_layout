jinja_extensions:
  - mod  # 添加 .mod 扩展名到处理列表
# 用户输入参数
project_name:
  type: str
  help: "请输入项目名称"
  default: "my_project"

module_path:
  type: str
  help: "Go模块路径 (例如: liuhuo23/{{project_name}})"
  default: liuhuo23/{{project_name}}